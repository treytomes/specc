# Fizz

Write a program called Fizz that iterates through every integer from 1 to 30.

For each number:

- If the number is divisible by 3, print **Fizz**.
- Otherwise, print the number itself.

The loop counter is an integer variable named `n`.
